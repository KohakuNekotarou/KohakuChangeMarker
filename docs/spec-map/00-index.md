# KCM 仕様地図 — 索引

**Kohaku Change Marker が「今どう振る舞うか」を、規則を1文ずつ並べた地図。**
コードの索引（`docs/ai-notes/kescm-file-map.md`）とは別物で、あちらが「どのファイルが何か」、
こちらが「製品として何がどう決まっているか」を持つ。

---

## この地図の目的

作者（＝あなた）が読んで、**頭の中の仕様と実装が食い違っているところを見つけるため**。
見つけたら、その場で書き込む。書き込まれたものは実装の変更依頼として扱われる。

---

## 訂正の書き方

各項目には空の `訂正:` 欄が付いている。そこに書く。

```markdown
- **OV-07** 比較の対象は文書2つで、3つ以上は同時に比べられない。
  - 訂正: 3つ並べたい。将来やるかもしれないので「今は2つ」と書いておいて
```

- **書く場所は訂正欄でなくてもよい。** 規則の文そのものを直しても、項目ごと消しても、
  新しい項目を足しても構わない。**このフォルダは git 管理下にあるので、どう変えても検出できる。**
- ID（`OV-07` など）は**やり取りのための番号**。これで指せば話が早い。番号は詰め直さない。
- 途中に項目を足すときは**枝番**（`OV-01b`）を使い、既存の番号は動かさない。
- ★**書き方は気にしなくてよい。走り書きで構わない。**
  **それを規則文として整った日本語にするのは私（Claude）の仕事**（2026-09-05 のお約束）。
  訂正が反映されると、訂正欄には `✅<日付> 対応` と、どこへ反映したかだけが残る。

---

## ★印の意味

**★が付いた項目 = 過去に「意図的にこう決めた」と記録が残っているもの。**

そこに訂正が入った場合は、決定を覆すことになるので、**実装する前に必ず確認を取る**。
「なぜそう決めたか」を添えて聞くので、その上で判断できる。

---

## 私（Claude）が次のセッションでやること

**この地図を開いたら、まず訂正を拾う。**

```
cd source/sdksamples/KCM
git diff -- docs/spec-map/          # 未コミットの書き込み
git log -p -- docs/spec-map/        # コミット済みの書き込み
```

拾った訂正は、**手を動かす前に3つに分類する**。分類は実コードを読んで決める。
訂正を額面通りに受けてコードを触らない。

| 種別 | 中身 | すること |
|---|---|---|
| **A** | 地図が実装を読み違えていた | **地図を直す**（コードは触らない） |
| **B** | 地図は実装と合っており、実装のほうが意図と違う | **コードを直す** |
| **C** | ★印の項目への訂正（過去の決定を覆す） | **先に理由を添えて確認**を取る |
| **Q** | 「こうしてほしい」ではなく**質問** | **実装を読んで答える。** 答えた結果が A/B/C のどれかなら、そのまま手を当てる |
| **F** | **今は無い機能が欲しい**（要望） | 地図に `⬜未実装の要望` として枝番で残し、**実装は別途提案してから**。地図は「今の実装」を写すものなので、要望を規則文に混ぜない |

★★**訂正欄には質問を書いてよい。**「これはこうなっているのでは？」で構わない ―― 答えを探すのは私の仕事。
実際、その形の質問1つが地図の不正確を1件あぶり出した（OV-17・2026-09-05）。**質問は指摘と同じだけ役に立つ。**

対応が済んだら、**規則文を新しい実装に合わせて書き換え、訂正欄に `✅<日付> 対応` を残す。**
この地図は常に「今の実装」を表す ―― それが崩れると、次の走査で同じ訂正をもう一度拾ってしまう。

---

## 章の一覧

| # | 章 | 状態 |
|---|---|---|
| 01 | [全体像](01-overview.md) | ✅ |
| 02 | 比較の起動と停止（Target/Source の決まり方・Start/Stop・外部 Source） | ⬜ |
| 03 | Pixel 変更モード ― 何をどう比べるか | ⬜ |
| 04 | Pixel のマーク ― 画面に何が出るか | ⬜ |
| 05 | ページの手動フラグ（登録 Added/Removed・✓・猫の手スタンプ） | ⬜ |
| 06 | あふれ（overset）の検出 | ⬜ |
| 07 | Story 変更モード ― 何を検出し、どう差分するか | ⬜ |
| 08 | Story Edits の一覧 UI（行・ツリー・クリック・ナビ） | ⬜ |
| 09 | Story のマーク（本文に敷く色地） | ⬜ |
| 10 | ブック比較 | ⬜ |
| 11 | マークの出力先（画面・印刷・PDF の違い） | ⬜ |
| 12 | パネル UI 全体（ステータス行・スクロール地図・サムネイル・半透明） | ⬜ |
| 13 | ツール・カーソル・マウス操作 | ⬜ |
| 14 | ビュー同期（Sync Layout Views / Align Other Views） | ⬜ |
| 15 | 隠す・書き出す（Hide Unchanged・変更ページ TSV） | ⬜ |
| 16 | 設定の保存と復元 | ⬜ |
| 17 | スクリプトに公開している口 | ⬜ |
| 18 | 文書のライフサイクル（閉じる・保存・dirty にしない約束） | ⬜ |

---

## 作り方の約束

- **憶測で書かない。** 実装（`.h` の契約・`.fr` の宣言・`.cpp` の分岐）を読んで確認したものだけを規則にする。
  memory や過去のノートは入口としてしか使わず、必ずコードで裏を取る。
- **読み切れなかった点は消さずに残す。** 各章の末尾に `⬜ この章で未確認` として並べる。
  空欄を「そんな決まりは無い」と読ませないため。
- 章の冒頭に、その章がコード地図のどのブロック（M3・U5 等）にあたるかを1行入れる。

---

**基準**: `feature/paw-stamp` の作業ツリー（未コミットの変更を含む）。版数 `2.1.0`。
